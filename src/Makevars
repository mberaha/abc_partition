CXX_STD = CXX14

SOURCES = abc_py_class.cpp distance.cpp distributions.cpp wasserstein.cpp rcpp_functions.cpp
OT_SRCS = ot/emd.cpp ot/sinkhorn.cpp
ALL_SOURCES = $(SOURCES) $(OT_SRCS)

OBJECTS = RcppExports.o $(ALL_SOURCES:.cpp=.o)

PKG_CXXFLAGS = -I. -I./ot -fopenmp -g

all: $(SHLIB)

$(SHLIB): $(OBJECTS)
